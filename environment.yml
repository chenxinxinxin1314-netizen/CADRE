name: cadre
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - torch>=2.0
      - torchvision>=0.15
      - numpy>=1.24
      - Pillow>=9.0
      - matplotlib>=3.6
      - tensorboard>=2.10
      - tqdm>=4.64
